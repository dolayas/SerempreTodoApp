import { createMuiTheme, Theme } from "@material-ui/core/styles"

const theme : Theme = createMuiTheme ({
    palette :
    {
        primary : {
            main : "#5C70BE"
        }
    }
})

export { theme }